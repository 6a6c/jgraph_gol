#include <vector>
#include <string>

std::vector <std::vector <int> > readGol();
void printGol(std::vector< std::vector <int> > &board);
void writeGol(std::string fn, std::vector< std::vector <int> > &board); 
void nextGol(std::vector <std::vector <int> > &board);
